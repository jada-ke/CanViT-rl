# data/

Sample/generated datasets used for local development and quick smoke tests.

- `data/synthetic_segmentation/`: small synthetic ADE20K-style dataset, generated
  by `scripts/synthetic_dataset/generate_synthetic_dataset.py`. Checked in
  so training/eval scripts have something to run against without a full
  ADE20K download.
